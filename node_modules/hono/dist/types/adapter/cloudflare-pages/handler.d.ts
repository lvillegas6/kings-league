import { Hono } from '../../hono';
import type { Env } from '../../types';
declare type EventContext = {
    request: Request;
    waitUntil: (promise: Promise<unknown>) => void;
    env: {} & {
        ASSETS: {
            fetch: typeof fetch;
        };
    };
};
interface HandleInterface {
    <E extends Env>(app: Hono<E>, path?: string): (eventContext: EventContext) => Response | Promise<Response>;
}
export declare const handle: HandleInterface;
export {};
